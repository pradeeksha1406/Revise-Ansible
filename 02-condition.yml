- name: Demo on condition
  hosts: localhost
  tasks:
    - name: print apple price
      ansible.builtin.debug:
        msg: Apple price - 1$
        where: The fruit == "Apple"

    - name: print orange price
      ansible.builtin.debug:
        msg: Apple price - 2$
        where: The fruit == "orange"